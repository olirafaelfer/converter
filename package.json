{
  "name": "hls-media-auditor-v10-defense",
  "version": "10.0.0",
  "description": "Local media/security auditor for owned sites: media discovery, cookie audit, HLS checks, full URL leak hunter, and defense report.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.19.2",
    "puppeteer": "^24.15.0"
  }
}
